- paho-mqttpp3:
    <<: *thirdparty_defaults
    version: 0.0.0.0
    mode: dr
    version_manager: git
    post_install:
        - ./src/mqtt/*.h include/mqttpp
    mode: dr
    source: https://github.com/eclipse/paho.mqtt.cpp.git
    cmake_definitions:
        - BUILD_SHARED_LIBS=ON
        - BUILD_TESTING=OFF
        - BUILD_STATIC=OFF
        - BUILD_SHARED=ON
    targets:
        - paho-mqttpp3:
            info:
                <<: *library_dynamic_exact
